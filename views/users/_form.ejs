<script>
   function confirmPassword() {
      var pw1 = document.getElementById('user_password').value;
      var pw2 = document.getElementById('user_confirm_password').value;
      if (!pw1 || !pw2) {
         alert('Debe rellenar los campos de Password y Confirmación.');
         return false;
      }
      if (pw1 === pw2) {
         return true;
      } else {
         alert('Los passwords introducidos no coinciden.');
         return false;
      }
   }
</script>


<div class="form-group">
	<% if ( ! user.id) { %>
		<label class="control-label col-sm-2" for="preg">Username:</label>
      <div class="col-sm-10">
		 <input class="form-control" type="text" id="username" name="user[username]" value="<%= user.username %>"/><p>
      </div>
	<% } else { %>
      <label class="control-label col-sm-2">Username:</label>
      <div class="col-sm-10">
		 <%= user.username %>
      </div>
	<% } %>
</div>

<div class="form-group">
	<label class="control-label col-sm-2" for="user_password" class="rem">Password:</label>
   <div class="col-sm-10">
	  <input class="form-control" type="password" id="user_password" name="user[password]"/> <p>
   </div>
</div>

<div class="form-group">
	<label class="control-label col-sm-2" for="user_confirm_password" class="rem">Confirm Password:</label>
   <div class="col-sm-10">
	  <input class="form-control" type="password" id="user_confirm_password" /> <p>
   </div>
</div>

<div class="form-group">
   <div class="col-sm-offset-2 col-sm-10">
   	<a href="/"><button class="btn btn-primary" type="button">Cancel</button></a>
   	<input type="submit" class="btn btn-primary" value="Save" onclick="return confirmPassword()">
   </div>
</div>